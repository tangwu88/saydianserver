# 支付宝手机网页跳转竞态修复

## 范围与结论

- 生产订单 `SD20260912041159FC1DDC72` 已创建，对应 ¥9.80 支付宝手机网页支付单仍为等待执行；未把失败跳转误判为付款成功。
- 前端提交支付宝表单后又立即查单。手机浏览器开始离开页面时中断该请求，因而显示底层 `request:fail`。
- 本轮只修复支付跳转与查单的时序和底层网络错误文案，不改订单金额、支付凭证、支付状态或其他业务流程。

## 实现

- `FORM` 和 `REDIRECT` 调用明确返回正在离页标记。
- 结算页与订单详情页收到该标记后不再立即查单或启动轮询；支付宝返回订单页后仍按原有服务端验签通知与查单流程确认结果。
- `request:fail`、`Failed to fetch` 等底层网络错误统一显示为可理解的网络提示。

## 验证记录

| 检查 | 结果 |
| --- | --- |
| 生产后台只读核对订单与支付单 | 订单待付款；支付宝手机网页 ¥9.80，等待执行 |
| `pnpm --filter @saydian/app-shop test` | 122 项通过 |
| `pnpm --filter @saydian/app-shop typecheck` | 通过 |
| `node tools/build-global-h5.mjs` | 通过 |
| 新增回归单测 | 首次暴露 VM 跨上下文对象原型的断言误报；网络提示单测同时暴露跨上下文 `Error` 识别不足，改为安全读取 `message` / `errMsg` 后全部通过 |

## 上线后验收边界

- 公网版本需确认 readiness revision 与提交一致，并检查订单详情页资源已更新。
- 真实支付宝 WAP 付款仍需用户完成一次小额支付，确认支付宝页可打开、异步通知或签名查单将订单更新为已付款；未获得真实成功回执前不得宣称支付全链路已验证。
