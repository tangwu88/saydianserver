# 国际版 CN/CNY 商城独立数据库验收

## 范围与安全边界

- 主任务授权本轮开放国际版中国地址/人民币商城；本子任务只新增本地验收工具，不修改服务、前端、生产配置或国内验收默认流程。
- 完整阅读 AGENTS、handoff、最近 H5 实施记录以及原隔离注入方案；fetch 后本地/远端独立分支均为 `0f9c7e0457e9ab554d92b72a913cd706ec63e09f`，保留全部在途修改。
- 使用已有 repo 外私有 demo profile，经原校验函数验证只连接 loopback `saydian_h5_demo`；不改写 profile。APP_REALM=global 和新签名材料仅在本次独立进程内有效，不复用当前 8081 的会话。
- 随机 loopback 端口，只关闭自己创建的 Nest 实例；启动前后核对 8080/8081/5173/5174/5175/5189/8091 PID 与启动时间。不启动 Worker，不读取或保存供应商密钥，不发送真实短信或微信请求。
- 网络限制先于 Prisma/Nest 加载；HTTP 额外仅允许本次随机端口。Prisma native 数据库连接由既有严格 profile URL 和 current_database 双重校验约束，不声称是操作系统级网络沙箱。
- 使用随机 marker 新增合成邮箱/手机号会员与 LOCAL 商品。短信验证状态作为明确的合成 fixture 写入，不把它当作真实手机验证证据。
- 真实支付服务及 `PaymentProviderService.identity/create` 保留，仅替换该 provider 的配置数据源与最终微信/支付宝传输为内存适配器。真实数据库集成状态仍为未配置，不写全库配置，不新增生产模拟支付端点。
- 清理在 Serializable 事务内复核 marker/创建时间/SKU 归属、其他会员订单/购物车/收藏/评价引用；只删除本轮未付款订单、内存适配器支付及其依赖，最后校验数量为零。

## 执行与结果

- 新增工具 `tools/h5-global-commerce-integration.mjs` 及独立源码 guard 测试；`node --check tools/h5-global-commerce-integration.mjs`、`node --test tools/h5-global-commerce-integration.test.mjs` 4/4 通过。
- 主任务追加授权可选 `--preview`：8091 必须原本空闲；自动验收通过后新建独立浏览器合成已验证邮箱用户，打印仅本地测试凭据，禁用内存支付身份，使预览阶段不能写入付款关系。主任务显式发送 `finish` + 换行后关闭自身实例并执行同范围清理；不用强停进程代替清理。
- 预览协议为本实例内存的两份显式 SYNTHETIC LOCAL QA 文档、随机版本和真实 ContentController 响应；不写入或覆盖共享数据库 GlobalLegalDocument，不改变其他运行实例的协议版本。该夹具不是正式法律审阅证据。
- 服务端就绪后执行 `RUN_H5_GLOBAL_COMMERCE_ACCEPTANCE=1 node tools/h5-global-commerce-integration.mjs F:/xcodeplace/saidianserver-local-runtime/h5-global-review-20260911/.env.h5-demo --preview`。
- 自动阶段通过：142 个断言、43 次 HTTP、内存支付发送 1 次；run=`3a2288b1-e585-4593-ae76-f905d2b0d36b`。覆盖已验证邮箱/国际手机号真实密码登录、未验证账号商城登录 403、App 未验证会话与临时 JTI 会话交易拒绝、CN+E164 地址、加购/报价/条件订单/幂等、越权查看拒绝、取消返库存、非 CN 无订单/库存副作用、CNY 支付金额/重试不重复及 USD/健康报告/原生与小程序支付拒绝。
- 8091 按授权保留供主任务浏览器联调；浏览器使用独立合成邮箱会员和本轮测试商品，不记录其随机密码。主任务完成邮箱登录返回商品、两次加购累计 2 件、CN+86 地址、39.98 元待付款订单刷新仍一单、未配置支付禁用、取消和退出；浏览器实际证据由主任务另记，不计入本工具 HTTP 次数。
- 收到主任务结束指令后向专用进程发送 `finish` + 换行，正常退出码 0：收尾 167 断言、43 次工具 HTTP、`cleanup=true`（仍为 run `3a2288b1-e585-4593-ae76-f905d2b0d36b`）。包括浏览器订单 `d07406e6-69cb-47c9-960f-7eb7ba5ee88e` 在内的本轮夹具经过 marker/归属/时间/状态检查后清理；只关闭自身 8091 Nest，不停其他 API/UI。
- 限制：本次替换的配置身份不验真实商户密钥完整性、签名和供应商回执；新增真实凭据政策由后端定向测试另验。不得将这次内存适配器支付写为真实微信支付成功。
- 主任务要求候选冻结后再终验：工具已改为在自动阶段调用真实 `super.identity(channel)` 和真实 `create()`，仅为该 provider 注入内存合成 CONFIGURED 行与运行时随机生成的 RSA/32 字节 APIv3 测试材料，不写数据库或文件、不输出密钥。新增缺少 APIv3 字段时付款 HTTP 503、PaymentIntent=0、传输调用=0，然后恢复合成字段使用同一请求键成功的场景。浏览器仍在旧实例，未热改其配置；最终执行结果待预览清理后补记。

## 冻结候选最终复验

- 主任务确认后端候选冻结且浏览器结束后，执行同一 opt-in 命令但不带 `--preview`；独立随机 loopback 端口，退出码 0。
- 最终结果：166 个断言、43 次工具 HTTP、内存传输 1 次、`cleanup=true`，run=`995865c3-bd3a-4669-9283-88a3619ff1a2`。未并行运行根级测试/构建/契约命令。
- 新门槛实际执行：缺少本轮内存 APIv3 字段时 HTTP 503，PaymentIntent=0、传输调用=0；恢复字段后相同请求键成功，再次重试只返回同一支付单。真实 `identity/create` 检查配置结构、CNY、商城业务和允许渠道；终点仍是内存传输，不进行签名出站或证明供应商可用。
- 非 CN 地址的 preview/create 均收紧为精确 `400 delivery_country_unsupported`，且订单数量/库存不变。两个已验证身份、未验证/临时会话拒绝、订单取消/越权/幂等均通过。
- 独立只读收尾复核两轮精确 marker：会员/商品/订单/支付单均为 0（包括浏览器订单精确 ID）；原 6 件 `H5-DEMO-LOCAL-` 商品仍在，存储供应商密钥仍为 0。两次工具均正常关闭自身实例，保留原监听 PID/启动时间。
- 语法、4 项独立工具 guard 测试及 `git diff --check` 通过。文件未提交、未部署；旧国内 271 断言报告保持独立，不与本轮数量相加冒充同一验收。

## 交付保留的新预览（第三轮，尚未清理）

- 主任务为交付可操作的本地购物页，明确要求再次运行冻结版 `--preview` 并保留待付款购物场景。重复执行上述 opt-in 命令带 `--preview`；8091 原本空闲，8081/5174/5189 的 PID 不变，没有修改其他服务或真实配置。
- 新 run=`63b9ff4e-bd80-4925-890d-4f28af9c10bf`，marker=`h5-global-commerce:63b9ff4e-bd80-4925-890d-4f28af9c10bf`；自动阶段 150 断言、44 次 HTTP、内存付款传输 1 次通过。该次还没有执行清理，不能以此前两轮清理结论宣称当前测试库所有夹具为零。
- 本地预览 API `http://127.0.0.1:8091`，进程会话 `55993`。专用浏览器合成邮箱与随机密码已私下回传主任务供交付，密码不写入日志。昵称继续保留精确 marker，因为现有安全清理依赖该值，不为友好展示削弱归属校验。
- 本轮专用商品 ID `5a853ef3-6566-45e3-9451-d0017661579e`，SKU ID `5a8db522-578c-4d3d-883c-0a328097eca6`，价格 1999 分；自动阶段结束后库存为 18。标题明确 GLOBAL QA 测试、不发货；浏览器账户没有预建真实付款关系。
- 自动阶段结束即强制关闭 preview 的付款 identity，公开支付保持未配置；仅可测试购物车、CN 地址、待付款订单和取消，不发生真实微信/支付宝/短信出站。
- 按主任务指令保留本轮进程与随机夹具，等待明确结束。结束时向进程会话 `55993` 标准输入发送 `finish` 加换行（例如工具 `write_stdin` 的 `chars: "finish\r"`），等待正常退出与 `cleanup=true`，而不是强停进程或批量删除。清理仅针对本轮 marker 及经验证归属的依赖，前两轮已清理结果保持有效。
