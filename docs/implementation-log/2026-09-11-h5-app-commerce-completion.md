# H5 顾客商城补齐与购物闭环验收

## 范围与成功标准

- 请求：H5 具备 App 商城相关功能，走通购物流程。仅在国际服务端 `saydian-server-global` 继续完善现有 uni-app H5、共享 API 与后台；不改国内客户端、旧域名、生产库或集成密钥。
- 修改前执行 `git status --short --branch`、`git -c credential.username=tangwu88 fetch origin --prune`、`git rev-parse HEAD origin/codex/global-api-foundation`。双方均为 `0f9c7e0457e9ab554d92b72a913cd706ec63e09f`；保留此前购物流程改动和独立 ERP 文件，不提交、不部署。
- 国际 App 只读基线 `1023ce4`。其当前国际商城实际支持目录浏览，继承的旧交易方法被 global 客户端拒绝，不能把这些界面存在当作交易已开通。本轮 H5 实现完整顾客购物链，不照搬旧整数 ID、money1 或国内接口。详见 [App 对照矩阵](2026-09-11-h5-order-groups-and-app-parity.md)。
- 成功标准分层：真实页面可操作；真实本地 API/数据库事务正确；支付、退款使用本实例的合成渠道和真实签名回调验证。真实微信授权、商户支付、物流和私有对象存储另外验收，不以模拟代替。

## 功能与修复

| 顾客流程 | 本轮承接及修复 | 验证层级 |
| --- | --- | --- |
| 首页、分类、搜索、详情、SKU | 保留既有目录/库存/分类/排序、手机底栏与桌面导航；详情新增可操作帮助与售后入口 | 页面、既有行为测试 |
| 登录、地址、收藏 | 复用统一账号及真实验证门禁；收藏可取消、错误可重试、换账号不串数据 | 行为测试、合成账号浏览器 |
| 购物车、结算 | 服务端报价、库存和提交版本校验；重复提交/刷新恢复；长券名不再把“优惠券”挤成竖列 | 手机截图对比、事务与竞争测试 |
| 优惠券 | 增加可使用/已使用/已失效/领券中心；公开券服务端分页；行锁保护最后额度、已领取幂等，扣除员工赠券预留 | 32 项顾客资产定向测试及真实 HTTP 竞争 |
| 积分 | 真实已核验抵扣余额与分页流水；未知显示未获取；取消/成功退款返还，尾差留在原分摊规则 | 页面、实际数据库分次退款 |
| 付款、订单 | 保存订单号后再付款；能力未配置不造成功；待发货合并 PAID/WAITING_FULFILLMENT，售后按真实关联查而非当前状态 | 签名回调、订单筛选、刷新恢复 |
| 物流、收货、评价 | 支持多包裹及分包商品数量，读取 traceJson；收货后评价，详情回显已评价 | 两包裹浏览器操作、越权/重复测试 |
| 商品售后 | 数量/现金/积分分开；同幂等键原文重放防丢响应再申请；旧版本冲突重新核对；可寄回物流，登记不等于商家收货 | 浏览器申请/登记、三次部分退款 |
| 售后图片 | 9 张、10 MiB、JPEG/PNG/WebP，校验真实头尾格式及归属；私有 Bearer 读取；后台关联权限与审计 | API/H5/后台定向测试；真实 S3 未配置 |
| 帮助客服 | 个人中心与商品页均可进入；展示已发布政策与实际客服信息，未取得联系方式不虚构 | 手机页面 |

员工佣金/提现不属于本轮顾客购物闭环的已验收范围；没有向生产 ERP、支付、短信或物流出站。

## 手机端人工操作证据

使用内部浏览器临时手机视口，真实 H5 页面访问隔离 API。没有注入 WeixinJSBridge，没有把浏览器模拟视口称为微信真机。

以下图片由浏览器截图直接保存并重新打开检查，目录 `artifacts/h5-app-commerce-20260911` 被 Git 忽略；没有图像编辑或伪造页面数据。

| 步骤 | 观察结果 | 截图 |
| --- | --- | --- |
| 帮助 | 已有政策/订单入口，无电话时友好提示 | [帮助](../../artifacts/h5-app-commerce-20260911/01-customer-help-phone.png) |
| 领取并查看券 | 合成公开券已领取，可使用列表同步 | [领券](../../artifacts/h5-app-commerce-20260911/02-coupon-claimed-phone.png)、[已领券](../../artifacts/h5-app-commerce-20260911/03-owned-coupon-phone.png) |
| 结算 | 19.99 − 1.99 券 − 6.67 积分 + 5.99 运费 = 17.32 元 | [修改前缺陷](../../artifacts/h5-app-commerce-20260911/04-server-coupon-points-quote.png)、[同视口修复后](../../artifacts/h5-app-commerce-20260911/05-quote-coupon-label-fixed.png) |
| 下单与刷新 | 单号保持 SD202609110745485E005FDA，未配置支付时保留待付款，不重复创建 | [订单已保存](../../artifacts/h5-app-commerce-20260911/06-order-persisted.png) |
| 取消 | 页面为已取消，取消操作消失，积分账本新增 +6.67 元 | [订单操作](../../artifacts/h5-app-commerce-20260911/07-order-cancelled.png)、[积分返还](../../artifacts/h5-app-commerce-20260911/08-points-returned.png) |
| 履约评价 | 两个包裹各 1 件；确认收货后评价，显示已评价而不是第二次提交入口 | [包裹](../../artifacts/h5-app-commerce-20260911/09-two-packages.png)、[已评价](../../artifacts/h5-app-commerce-20260911/10-review-submitted.png) |
| 商品售后 | 选择 2 件中 1 件，申请现金 18.32、积分 1.66；保存后待审核，不显示到账 | [售后已提交](../../artifacts/h5-app-commerce-20260911/11-after-sale-applied.png) |
| 寄回 | 保存合成运单后仍待寄回/待商家确认，不自动退款 | [寄回信息](../../artifacts/h5-app-commerce-20260911/12-return-logistics-saved.png) |
| 私有图片能力 | 新 API 已实际返回关闭状态，按钮禁用，但仍能提交文字申请；不泄露接口技术错误 | [友好提示](../../artifacts/h5-app-commerce-20260911/13-private-images-disabled-safe.png) |
| 响应布局 | 同一分类页桌面 1280、平板 768、手机 390 视口；侧栏/网格/底部四入口正常，无横向挤压；真实品牌图只读复用 | [桌面](../../artifacts/h5-app-commerce-20260911/14-desktop-category.png)、[平板](../../artifacts/h5-app-commerce-20260911/15-tablet-category.png)、[手机](../../artifacts/h5-app-commerce-20260911/16-phone-category.png)、[详情](../../artifacts/h5-app-commerce-20260911/17-phone-product.png) |

所有账号、订单、价格、库存、券、积分、姓名、电话、运单均为独立合成测试数据，不是真实成交或客户评价。

## 检查与问题记录

- 首次完整交易：411 项断言 / 108 HTTP；追加取消、权限、分包物流后：440 项 / 113 HTTP，清理成功。覆盖相同及不同事件号重复签名回调，三次单件退款 633 分现金、333/333/334 积分，零现金商品项只返积分，运费另审，越权和版本冲突。详见 [完整交易记录](2026-09-11-global-commerce-full-cycle.md)。
- 本轮第一次根 `pnpm typecheck` 通过（全部工作区）。最终字段文档修改并生成后，第二次根类型检查亦通过。
- H5 顾客资产定向 10/10；API 顾客资产 32/32；图片、售后和导航定向 H5 42/42、API 47/47、后台私有查看 4/4 通过。
- 浏览器长优惠券测试发现结算标题被压成竖列；固定 label 不收缩、picker 容器允许换行；同视口同报价对比通过。
- 在 API 旧运行快照中，新图片能力路径暂时 404；没有伪造上传成功。完成其余交易操作后更新独立实例，再复核能力关闭状态。不会改生产对象存储。
- 浏览器直接改变同一路径 hash 的订单 ID 时，uni-app 复用原页；本次通过实际订单列表点击进入对应订单完成验收，未把旧内容当成另一订单通过。
- `pnpm api:docs`：320 条路由均有说明。新增图片 GET 正确导出 JPEG/PNG/WebP 原始二进制，multipart 文件示例使用 `--form`，非文件字段保留 `--form-string`，不会生成假公开图片链接。
- `pnpm test`：全部工作区退出 0；API 678 通过、4 项条件数据库测试跳过（其专用开关未开），后台 96 通过。这里的跳过不冒充成功；另有真实 PostgreSQL 的独立 440/454 断言验收。
- `pnpm tools:test`：9 工具 + 61 H5 恢复/跨页流程 + 38 契约/隔离保护测试全部通过。
- `pnpm build`：全部工作区构建成功；存在现有 Sass legacy/@import 弃用及后台大 chunk 警告，没有为了消除警告升级无关依赖。
- 随后显式设置 `VITE_APP_REALM=global`、`VITE_API_BASE=/global/api/saidian-mall/v1`、`VITE_PUBLIC_BASE=/global/saidian-mall/`，运行 `pnpm --filter @saydian/app-shop build:h5` 通过；当前 H5 构建产物为国际版本，不留根默认国内构建作为交付。
- 最后 `pnpm api:docs:check`（320 路由）、`pnpm contracts:client:check`（冻结客户端 fa79aa3，76 个消费者路径/方法，missing=[]）、`git diff --check` 均通过。客户端契约仅路径存在性，不冒充 Flutter 字段解析/真机已验收。最终复核 Git 工作区仍保留所有原有修改及独立 ERP 文件；没有暂存/提交/推送。

## 运行与交付边界

- 隔离库仅 `127.0.0.1:5432/saydian_h5_demo`，工具二次核验库名并禁止外部网络；只清理本轮明确 ID、marker、时间和签名支付来源的合成实体，保留其他演示/旧预览。
- 自动完整验收命令见 `global-commerce-full-cycle`；`--journey-preview` 额外保留两个合成订单用于手机演示，交付浏览器前禁用所有付款渠道。输入 `finish` 正常退出并执行严格清理，不能用杀进程代替。
- 沿用项目 UI，界面审查技能用于“原截图/同视口对照/真实点击”，未另建站点或重画品牌。
- 本轮没有发布线上。真实商户支付退款、公众号真机、对象存储上传回执、真实物流/ERP、真实供货库存和售后再入库规则仍需分别验收。

## 本地预览收尾

- 第一轮浏览器 run `f9c9dcef-3a11-4c88-963b-9de56adbcb03` 已正常输入 `finish`，总计 499 断言且 `cleanup=true`。仅本轮合成会员、商品、订单、退款、评价和关联账本清除；可重跑脚本创建新的测试数据，不是删除真实业务记录。其他历史 demo/旧 8091 原样保留。
- 第二轮冻结代码 run `5fa8b336-6904-4e4c-95b3-b68f4d0a9edb` 在浏览器前完成 454 断言 / 136 HTTP / 5 合成付款 / 4 合成退款。新售后图片能力真实 HTTP 验证：匿名 401、正式测试会员 `enabled=false`、无 FileObject 写入；不存在生产模拟付款端点。
- 当前预览 `http://127.0.0.1:5177/global/saidian-mall/#/pages/category/index`，仅代理 API `127.0.0.1:7126`。H5 会话 `85130`、API 会话 `10964`，为用户查看而保留；旧 `5176 → 8091` 没有关闭。新实例的合成账号在内部浏览器已登录，随机密码不写文档。
- 停止此预览时，先向 API 会话 `10964` 输入 `finish` 加回车，必须看见 `cleanup=true` 再关闭 H5 会话 `85130`。若出现任何非本轮实体引用，清理会拒绝，不能放宽保护条件或直接删库。用户如继续演示，购物只操作本轮 `GLOBAL QA 5fa8b336` 商品；其他演示商品仅浏览，避免混用夹具影响清理。
- 内部浏览器保留新旧预览，临时视口已 reset 恢复正常；最终新预览采集的 error 级浏览器日志为空。已打开的用户后台/微信/ERP 标签页没有关闭。

## 结算页发票字段精简

- 用户要求下单时不填写发票抬头；已从 H5 结算页移除整个“发票抬头”输入项，并从创建订单参数中移除 `invoice`，避免仅隐藏界面但继续提交旧值。服务端原有可选发票能力未删除，买家留言能力仍保留。
- 为缩短默认结算流程，订单备注改为按需展开；不需要备注的顾客不会看到输入框。服务端报价、优惠券、积分和库存复核仍保留，不能为少一步而绕过交易校验。
- 收货地址将 H5 原来的省、市、区三个自由输入框合并为一个中国大陆省市区联动选择器；保存行政区名称和代码，记住本账号上一次所选地区。新增必填项进度、逐项错误提示、浏览器姓名/电话/街道自动填充属性，保存成功后直接作为结算地址。
- 商品详情新增“分享商品”入口：普通手机优先使用系统分享，微信内明确引导右上角分享，其他浏览器复制规范商品链接；不附带会员、订单或推广身份信息。
- 商城测试 107/107、商城 `vue-tsc --noEmit`、国际 H5 构建、冻结锁文件离线安装和 `git diff --check` 均通过。内部浏览器实际确认：结算页无发票字段、备注默认折叠；地址页只保留四类必要信息且三级选择器可打开；商品页分享入口可见。
