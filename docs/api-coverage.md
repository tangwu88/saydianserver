# 原后台功能对接与缺陷清单

核对日期：2026-09-05。依据：Flutter 基线 `b671e7f`、原商城基线 `09963c4`、旧 H5/小程序源码、本仓库控制器/服务、原后台可见功能和已生成接口目录。未获得旧 App/商城生产库只读盘点及供应商真实回执，不能将源码迁入称为生产数据迁移或全链路验收。

## 状态定义

- **已修复并单测**：对应回归测试通过；不等于手机、供应商或迁移数据验收。
- **有实现，待联调**：存在真实存储/适配逻辑，仍需真实环境验证。
- **未接通**：需要凭据、数据迁移或业务配置；不能给用户伪成功。
- 生产服务当前维持维护只读。本轮自动发布不得打开写入，不改旧 `app.saidian.cc`。

## 模块覆盖

V2 以下省略 `/api/saydian-app/v2`；完整方法、参数、鉴权和响应见 [调用指南](api-guide.md) 与 [269 条路由目录](api-reference.md)。

| 业务 / App 对应入口 | V1 兼容入口 | V2 / 数据来源 | 本轮结果与剩余边界 |
|---|---|---|---|
| 登录、注册、退出、刷新 | `/api/v1/site/*` | `/auth/*`、User/UserSession | multipart 解析修复；真实短信和旧会话兑换未接通；注册手机号验证见 P0 |
| 资料、头像、目标、注销 | `/api/v1/member/member/*`、`member-mubiao*`、`account/delete` | `/members/me*`、`/auth/delete-account` | 有存储与注销任务；旧会员迁移和完整注销演练未验收 |
| 健康同步、详情、历史 | `jrjk`、`daily-date`、各指标 `preview`、`health-records/batch` | `/health/records*`、HealthRecord | 修复零值污染、北京时间、旧历史截断、旧上传重复、缺失详情路由；多样本和 V2 游标仍有缺陷 |
| ECG | `/api/v1/member/e-c-g*` | `/files/ecg`、FileObject/EcgArtifact | 上传与索引存在；私有波形授权下载闭环缺失 |
| 健康预警 | V1 健康上传接入阈值处理 | `/health/warning-rules`、`/health/warnings` | 阈值、Outbox 有实现；关爱通知转发与真机推送未闭环，不输出诊断 |
| 健康档案、详细报告 | 旧客户端无对应契约 | `/health/profile`、`/health/reports*`、HealthProfile/HealthReport | 30 天证据、3 天门槛、单独同意、报告队列、权益、失败返还和按需 PDF 已实现并单测；Flutter 页面、AI 真实生成与付费端到端未验收 |
| 设备信息 | 以 V2 为准 | `/devices*`、DeviceBinding | 绑定、能力快照、游标有实现；手机硬件指令不在服务端执行 |
| 远程关爱、共享授权 | `care*`、`care-setting*` | `/care/*`、Relationship/Permission/Audit | 修复旧字段映射、重复邀请、撤销后的旧授权；到期与并发完整验收待补 |
| 消息、公告及未读 | `notify*`、新增 `notify/statistics` | `/notifications*`、Notification/NotificationCampaign | 补统计、详情已读、旧数字 ID 查询保护和后台定时活动；推送供应商未接通 |
| 推送设备 | `push-devices*` | `/notifications/push-installations` | 有安装实例、Outbox、活动分发和 JPush 适配；APNs、厂商通道、前后台/杀进程投递未验收 |
| 百科、分类、协议 | `/api/rf-article/*` | `/content/*`、Article/LegalDocument | 旧数字 ID 查询修复；新内容旧整数 ID、单页协议映射仍有缺口 |
| AI | `/api/rf-article/chat/*` | `/ai/messages`、Conversation/Message | 表单请求解析修复；供应商未接通，会话上下文需要完善 |
| 反馈、客服、更新 | 以 V2 为准 | `/support/*`、Feedback/AppSetting | 有保存与配置接口；附件阅读、客服回复闭环不足 |
| 商城首页、商品 | `/api/v1/pages`、`/api/inv-shop/v1/product/*`、`/api/saidian-mall/v1/storefront/*` | `/commerce/*`、CommerceProduct/Sku/Category/Banner | 商品与展示资料进入主库；后台禁止改 ERP 编号、内部名、SKU 和权威库存；聚水潭同步尚未真实验收 |
| 购物车、地址、订单、物流、售后 | `address*`、`/api/inv-shop/v1/order/*`、商城兼容路由 | CommerceCart/Address/Order/Shipment/AfterSale + LegacyOrderProjection | 一个会员 UUID、一个主库订单；预览重算、幂等、旧单只读有实现；生产数据迁移、ERP 写权限、物流/售后回执未验收 |
| 评价、优惠券、员工推广 | 商城兼容路由 | CommerceReview/Coupon/Employee/Commission/CouponGift | 真实评价只可上下架；员工企业微信、推广码、业绩、奖金、一次性赠券已实现；旧充值/提现只读，企业微信未真实联调 |
| 统一支付与退款 | `/api/v1/pay`、商城支付兼容路由 | `/billing/*`、PaymentIntent/Refund/ProviderEvent | 微信/支付宝/StoreKit 契约、回调验签、金额核对、去重、退款撤权已实现并单测；商户沙箱、Apple 通知和对账未真实验收 |
| 管理后台与接口中心 | 不复用旧后台登录 | `/api/saydian-app/admin/v1/*` | 会员、健康、商城、支付、通知、内容、集成、接口说明进入同一后台；角色守卫、密钥不回显、文档审核发布已实现，复杂运营交互仍见 P1 |

## 本轮已修复的兼容问题

1. 原 App/小程序 multipart 表单不能稳定进入 DTO：登录、会员、内容及商城路由使用受限的无文件表单解析器，文件走专用上传接口。
2. 旧健康记录的空串/null 变 0、无时区时间漂移、同小时记录相互覆盖：保留未知值，按真实时间/内容生成记录标识。
3. 旧历史沿用 V2 的 200 条上限：专用历史读取、日期范围及分页；超出安全上限明确失败，不静默截断。
4. 旧上传请求重试缺乏稳定去重、旧客户端不能理解部分失败：兼容层单独计算指纹、按 200 条分批，并明确拒绝失败批次。
5. 关爱授权旧指标拼写无法往返：集中映射；重复 pending 邀请保持原 ID；active 邀请不重建；撤销删除授权。
6. 缺失旧消息分类统计/详情已读、旧数字标识错误参与 UUID 查询：修复并增加归属测试。
7. 支付编号不匹配现有商城、旧订单误入新支付：规范化支付方式，在商城调用前阻断已识别的旧订单投影写入。
8. 补齐旧日表、身体成分、血液成分历史/详情路由，避免静态 preview 被动态 ID 路由抢占。

## 本轮主系统整合结果

1. 从原商城提交 `09963c4` 迁入 H5/小程序同源前端与成熟业务契约；新商城读写直接落主库，不再运行期调用第二套商城，原数字标识保留兼容映射。
2. 建立商品、SKU、分类、购物车、地址、订单、物流、售后、退款、评价、优惠券、员工推广、奖金、ERP 作业和重试模型；聚水潭权威字段只由同步任务更新。
3. 建立商城订单、单次健康报告、健康会员共用的统一支付单，集中处理微信、支付宝、StoreKit、供应商事件去重、退款和权益撤销。
4. 建立健康档案、确定性数据证据、AI 分析单独同意、报告资格、版本化价格、次数账本、30 天会员、串行生成、失败返还和按需 PDF。
5. 建立通知活动、目标人群、定时分发、失败重试和不含手机号/健康值的推送载荷；未配置供应商时不伪报送达。
6. 建立写入不回显的集成密钥中心，使用主机外置主密钥加密；API、Worker、文件、短信、AI、支付、企业微信、聚水潭和 JPush 共用配置边界。保存配置不等于已验证，只有真实成功调用才写入检测时间；容器重启不会用 seed 覆盖后台配置。
7. 建立接口中心：代码生成方法/路径/鉴权，业务说明可编辑，支持草稿、审核、发布、回滚、过期检测及脱敏 Markdown/OpenAPI 导出。
8. 新增原商城库只读盘点/迁移/校验入口；未连接真实旧库时只验证程序和脱敏映射，不生成虚假的数量或金额报告。

## P0：开放真实用户写入前必须解决

| 缺陷 | 影响 | 验收要求 |
|---|---|---|
| 注册尚未强制验证手机号，商城身份映射依赖手机号 | 不能证明账号拥有该手机号，可能错误匹配商城身份 | 完成短信验证/验证状态持久化，未验证账号禁止映射商城身份；双账号攻击用例通过 |
| 旧数据、密码兼容和旧会话兑换未完成真实盘点/迁移 | 旧用户、历史与登录无法平滑延续 | 只读盘点、迁移报告、数量/金额/附件 SHA 校验、灰度联调 |
| 短信、AI、极光/APNs、微信/支付宝/Apple、企业微信、聚水潭尚未完整配置 | 有适配代码不代表业务可用 | 分项写入密钥、真实检测、沙箱/供应商回执和对账；未配置保持真实错误 |
| 本轮数据库迁移尚未在生产备份上演练 | 直接自动部署可能在共享 4GB 主机失败或造成长锁 | CI 空库迁移通过后，在生产备份副本演练、核对时长/磁盘/回滚点并人工批准 |
| 当前维护只读 | 普通用户登录写入、资料、健康同步等会被维护中间件阻止 | 前述验收通过后单独决定开放时间，不由代码发布顺便开启 |

## P1：已确认的功能缺口

- **健康分页/重试**：V2 仅使用时间游标，同时间多条记录可能跨页遗漏；同 clientRecordId 不同内容的重复冲突与 storage_failed 的幂等重试策略需完善。
- **旧多样本**：旧日表 pulse/oxygen/HRV 数组当前取首项，尚不是无损多样本导入；无时间且无幂等键的旧上传无法可靠辨别重试与新测量。
- **私有文件**：ECG 波形和反馈附件缺少完整授权读取入口；仅检查上传 MIME 不等于校验真实文件签名。
- **关爱/预警**：shareWithCare 保存不等于已经转发给关爱人；到期失效、并发邀请、撤销竞态和推送需双账号完整测试。
- **内容**：新建文章/分类的 UUID 与旧 App 整数 ID 契约未完全统一；旧单页协议入口未完整映射 LegalDocument。
- **AI**：会话标识和供应商上下文仍需对齐，不能将当前单次请求当成完整多轮对话验收。
- **商城/ERP**：旧商品、SKU、会员、订单、支付、退款、附件和奖金尚未跑真实迁移对账；聚水潭现有读取授权不能证明订单/退款/售后写权限；库存预占、释放和 ERP 回执需并发验收。
- **支付/报告**：微信、支付宝、StoreKit 沙箱与退款通知尚未实测；Apple 价格单位、交易恢复、跨设备和退款通知需用真实产品验收；Flutter 尚无健康档案、报告预览、购买、恢复和历史页面。
- **推送/员工**：APNs 与各安卓厂商通道未接入；极光、企业微信 OAuth、推广归因和赠券分享需真实账号/杀进程/并发领取验证。
- **后台操作**：核心资源已有专页/操作，但富文本、商品规格批量查看、复杂售后处理、财务对账、客服回复和迁移冲突复核仍需增强；菜单可见性和服务端 RBAC 需十类角色逐账号验收；无完整自助改密/最后一位超级管理员保护。
- **接口中心**：代码签名和发布流程已实现，但生成的是路由级 OpenAPI，尚不是每个 DTO 字段的完整 JSON Schema；说明审核需用不同管理员账号验收。
- **管理与 App 实测**：尚未用迁移后的双账号、安卓及 iPhone 完成全业务测试。单元测试、CI 或健康探针均不能替代该验收。

## 运维与迁移待验收

- 本机无 Docker；真实数据库、HTTP 契约与镜像检查由 CI 提供证据。
- 当前主机为 4 核 / 4GB / 40GB，和其他应用共用；发布备份和镜像增长需监控。
- 附件与备份暂在本机磁盘/私有 MinIO；不属于异地容灾。自动备份脚本存在不等于恢复演练通过。
- 自动发布配置与首次线上结果以 [持续部署说明](continuous-deployment.md) 和逐轮实施日志为准，不能只依据 workflow 文件判断已启用。
- 本轮不重写健康算法、不搬移真实旧库、不切换旧域名；生产商城/支付/库存仍未切换到本轮新模型。
